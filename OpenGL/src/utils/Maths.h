#pragma once

class Maths
{
public:
	static unsigned short* AddVectors(unsigned int size, const unsigned short* v1, const unsigned short* v2);
	static unsigned short* AddVectors(unsigned int size, const unsigned short* v1, const unsigned short* v2, const unsigned short* v3);
};
